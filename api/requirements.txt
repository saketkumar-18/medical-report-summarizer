# Serverless deps for @vercel/python (installs from api/requirements.txt).
# Keep in sync with the root requirements.txt.
fastapi>=0.115
starlette>=0.38
pydantic>=2.7
python-multipart>=0.0.9
pypdf>=4.0
